# ╔═══════════════════════════════════════════════════════════════════════════╗
# ║                          VICTORY SCREEN                                   ║
# ║              Hiển thị khi player sống sót đủ thời gian                    ║
# ╚═══════════════════════════════════════════════════════════════════════════╝
#
# ┌─────────────────────────────────────────────────────────────────────────┐
# │ BÀI HỌC: Win Condition (Điều kiện thắng)                                │
# ├─────────────────────────────────────────────────────────────────────────┤
# │ MỌI GAME CẦN:                                                           │
# │   1. Win condition - Điều kiện để thắng                                 │
# │   2. Lose condition - Điều kiện để thua                                 │
# │   3. Feedback - Cho player biết họ thắng/thua                           │
# │                                                                         │
# │ VAMPIRE SURVIVORS STYLE:                                                │
# │   - Win: Sống sót 30 phút                                               │
# │   - Lose: HP = 0                                                        │
# └─────────────────────────────────────────────────────────────────────────┘

extends CanvasLayer


# ═══════════════════════════════════════════════════════════════════════════
#                              REFERENCES
# ═══════════════════════════════════════════════════════════════════════════

@onready var panel: Panel = $Panel
@onready var title_label: Label = $Panel/VBoxContainer/TitleLabel
@onready var time_label: Label = $Panel/VBoxContainer/TimeLabel
@onready var kills_label: Label = $Panel/VBoxContainer/KillsLabel
@onready var level_label: Label = $Panel/VBoxContainer/LevelLabel
@onready var play_again_button: Button = $Panel/VBoxContainer/PlayAgainButton
@onready var quit_button: Button = $Panel/VBoxContainer/QuitButton


# ═══════════════════════════════════════════════════════════════════════════
#                              INITIALIZATION
# ═══════════════════════════════════════════════════════════════════════════

func _ready() -> void:
	# Ẩn ban đầu
	visible = false
	
	# Connect buttons
	play_again_button.pressed.connect(_on_play_again)
	quit_button.pressed.connect(_on_quit)
	
	# Connect victory signal
	Events.victory.connect(_on_victory)


# ═══════════════════════════════════════════════════════════════════════════
#                           EVENT HANDLERS
# ═══════════════════════════════════════════════════════════════════════════

func _on_victory() -> void:
	# ┌─────────────────────────────────────────────────────────────────────┐
	# │ BÀI HỌC: Victory Feedback                                          │
	# ├─────────────────────────────────────────────────────────────────────┤
	# │ Khi player thắng, cần:                                              │
	# │   1. Dừng game (pause)                                              │
	# │   2. Hiện màn hình chiến thắng                                      │
	# │   3. Hiện stats (để player tự hào)                                  │
	# │   4. Cho phép chơi lại hoặc thoát                                   │
	# └─────────────────────────────────────────────────────────────────────┘
	
	# Pause game
	get_tree().paused = true
	
	# Lấy stats từ GameManager
	var total_time = GameManager.game_time
	var total_kills = GameManager.total_kills
	var player_level = GameManager.player_level
	
	# Format time
	var minutes = int(total_time) / 60
	var seconds = int(total_time) % 60
	
	# Update labels
	title_label.text = "🏆 CHIẾN THẮNG! 🏆"
	time_label.text = "Thời gian: %02d:%02d" % [minutes, seconds]
	kills_label.text = "Yêu Ma đã diệt: %d" % total_kills
	level_label.text = "Cảnh Giới đạt được: %d" % player_level
	
	# Hiển thị
	visible = true


func _on_play_again() -> void:
	# ┌─────────────────────────────────────────────────────────────────────┐
	# │ BÀI HỌC: Scene Reloading                                            │
	# ├─────────────────────────────────────────────────────────────────────┤
	# │ get_tree().reload_current_scene():                                  │
	# │   - Tải lại scene hiện tại từ đầu                                   │
	# │   - Tất cả nodes được tạo lại                                       │
	# │   - State reset về ban đầu                                          │
	# │   - Cách nhanh nhất để "restart game"                               │
	# └─────────────────────────────────────────────────────────────────────┘
	
	get_tree().paused = false
	GameManager.reset_game()
	get_tree().reload_current_scene()


func _on_quit() -> void:
	get_tree().quit()
